# config/.env — LLM API 密钥及其他敏感配置（不进 git）
# 将此文件复制为 config/.env 并填写真实值
# ⚠️ 确保 .gitignore 中包含 config/.env

LLM_API_KEY=sk-your-api-key-here

# NCBI GEO 爬取联系邮箱（NCBI 要求有效邮箱，否则有被限流风险）
GEO_CONTACT_EMAIL=your_email@example.com
